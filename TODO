-100 : Extend functionality of snmp subsystem to cover that of radctl.c module.
-100 : Add documentation for multiple interfaces/ports (listen statement, etc.)
-100 : Add documentation for NOREALM.
-100 : Add documentation and default configs for attr_query!
-100 : Add documentation for default attribute values in % substitutions.
-100 : query_buffer_size is obsoleted. Update docs accordingly.
-50  : Check for strcspn() and provide substitute if necessary.
-3   : Enable use of port number in rewrite:check_unix()
0    : Multiple servers per realm? (Rasmus Aveskogh <rasmus.aveskogh@utfors.se>)
2    : radprintf: Select more appropriate notations for (%I, %A). Maybe
       %#i, %#p would do: both are accepted by usual printf functions
       and do not make lint angry.
5    : Write a unified configuration utility in guile.
10   : Need a more flexible IP allocation strategy


URGENT! Restore the functionality of late radiusd/radctl.c module:

getpid:
	Add OID:

		radiusStatPID.

get-m-stat:
	Add OIDs:
                +- NumClasses
	        +- NumBuckets
                +- NumBytes
		+- MemTable
                   +- Index
                   +- MemEntry
                   +- Class
		   +- ElsPerBucket
	           +- Buckets
	           +- ElsUsed
                   +- ElsTotal
		+- MemUtilization (per 1000)
		+- MallocBlocks
		+- MallocBytes
		
get-q-stat:
	Add OIDs:
		+- QueueTable
	           +- QueueEntry
                      +- QueueIndex
                      +- QueueName (optional)
	              +- QueueActive
		      +- QueueHeld
	              +- QueueTotal

dumpdb:?
remark:?


*reload:
	Functional via radius{}ServConfigReset=reset

restart:
shutdown:
	Add to radius{}ServConfigReset new values:

		restart(5)	--	Cold restart.
		shutdown(6)	--	Shutdown.

divert
/* $Id$ */
#if defined(HAVE_CONFIG_H)
# include <config.h>
#endif
#if RADIUS_DEBUG
#include <log.h>

int debug_level[MODNUM];

struct debug_module debug_module[MODNUM+1] = {
undivert(1)
undivert(2)
	(char*)0, 0
};

#endif
